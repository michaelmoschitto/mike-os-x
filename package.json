{
  "name": "mike-os-x",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "rip:web": "bun --cwd apps/web dev",
    "rip:api": "cd apps/api && uv run fastapi dev main.py",
    "dev:infra": "docker compose -f docker-compose.dev.yml up",
    "dev:build:infra": "docker compose -f docker-compose.dev.yml up --build",
    "down": "docker compose -f docker-compose.dev.yml down",
    "down:clean": "docker compose -f docker-compose.dev.yml down -v",
    "logs": "docker compose -f docker-compose.dev.yml logs -f",
    "build": "bun --cwd apps/web build",
    "preview": "bun --cwd apps/web preview",
    "test:api": "cd apps/api && uv run pytest",
    "format:api": "cd apps/api && uv run ruff format .",
    "format:web": "cd apps/web && bun run format",
    "lint:web:fix": "cd apps/web && bun run lint:fix",
    "lint:api": "cd apps/api && uv run ruff check .",
    "lint:api:fix": "cd apps/api && uv run ruff check --fix .",
    "type-check:api": "cd apps/api && uv run mypy ."
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "pdfjs-dist": "^5.4.394"
  }
}
